
보조수단
dgv2_update 아래 루틴 호출 
dgv2_update_보유표시 one up one down two up two down three up three down
	    one, two, three 보유액 크기순
		 



가의 급락
	보유종목 시간별 변동기록
		가격
		프돈은 틱 단위 밖에 자료가 없다(marketeye_received only)

	소리 & 
	-> 비상매도 호출 -> 매도? -> y -> 시장가
											n -> 호출기록 보유 1분 동안 동일질문 배제, 종목 및 호출시간보유 List

	호가창 작동시 호가 변동 ->  매수1호가, 매도 1호가, 매수1호가량, 매도1호가량 반영
	stockjpbid3_Received의 결과를 deal_호가_추가()에서 호가창이 작동하는 3 종목의 경우 다음과 같이 업데이트
			o.매도1호가잔량 = (ulong)Convert.ToInt32(dtb.Rows[4]["매도"]);
			o.매수1호가잔량 = (ulong)Convert.ToInt32(dtb.Rows[5]["매수"]);
			o.매도1호가 = Convert.ToInt32(dtb.Rows[4]["호가"]);
			o.매수1호가 = Convert.ToInt32(dtb.Rows[5]["호가"]);
			o.현재가 = o.매수1호가;